extends ../layout.pug
include ../mixins/newspost.pug

block head
	title ニュース

block content
	h1.board-title ニュース
	if news.length === 0
		p.text-center ニュースはありません。	
	else 
		include ../includes/stickynav.pug
		each post in news
			+newspost(post)
