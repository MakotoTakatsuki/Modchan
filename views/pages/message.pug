extends ../layout.pug

block head
	title #{title}
	if redirect
		meta(http-equiv="refresh" content=`3;url=${redirect}`)

block content
	h1 #{title}
	ul
		if message
			li #{message}
		if error
			li #{error}
		if messages
			each msg in messages
				li #{msg}
		if errors
			each error in errors
				li #{error}
	if link
		div
			a.button(href=link.href target='_blank') #{link.text}
	if redirect
		p まもなくリダイレクトされます。自動的にリダイレクトされない場合は、#[a(href=redirect) ここをクリック]をご利用ください。
